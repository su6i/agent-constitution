---
محل قرارگیری: ریشه پروژه (root/AGENTS.md)
توضیح: این فایل حتماً باید در ریشه اصلی پروژه قرار بگیرد تا توسط ایجنت‌ها شناسایی شود.
---

# AGENTS.md — راهنمای Agent برای [نام پروژه] (نسخه تمپلت)


## خلاصه پروژه
[یک پاراگراف در مورد اهداف و ماهیت این پروژه بنویسید.]

## ساختار
- `src/` — کدهای اصلی
- `tests/` — تست‌ها
- `docs/` — مستندات
- `memory-bank/` — مدیریت کانتکست بین جلسات

## قوانین مهم برای ایجنت‌ها
- در هر لحظه فقط روی یک تسک تمرکز کن.
- قبل از پیاده‌سازی، حتماً تست بنویس (TDD).
- از ابزار مدیریت پکیج مناسب (مثل `uv`) استفاده کن.
- فرمت کامیت: `[نوع]: [توضیح کوتاه]`.

## نحوه اجرای تست‌ها
```bash
# مثال:
uv run pytest
```

## نحوه بازبینی کد (Lint)
```bash
# مثال:
uv run ruff check .
```
